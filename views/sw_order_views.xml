<odoo>
  <data>
    <!-- explicit list view definition -->
    <!-- <record model="ir.ui.view" id="sworkshop.list_order">
      <field name="name">Workshop Order List</field>
      <field name="model">sworkshop.order</field>
      <field name="arch" type="xml">
        <tree>
          <field name="customer_id"/>
          <field name="vehicle_id"/>
          <field name="opening_date"/>
        </tree>
      </field>
    </record>

    <record model="ir.ui.view" id="sworkshop.form_order">
      <field name="name">Workshop Order Form</field>
      <field name="model">sworkshop.order</field>
      <field name="arch" type="xml">
        <form string='Workshop Order'>
          <group>
            <field name="customer_id"/>
            <field name="vehicle_id"/>
            <field name="opening_date"/>
          </group>
        </form>
      </field>
    </record> -->

    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="sw_action_order_window">
      <field name="name">Workshop Order</field>
      <field name="res_model">sworkshop.order</field>
      <field name="view_mode">tree,form</field>
    </record>

  </data>
</odoo>
